language: python

python: 
  - "3.6"

install:
  - pip install pytest
  - pip install pytest-cov
  - pip install pytest-flake8
  - pip install coveralls

script:
  - python -m unittest discover
  - pytest --cov=test/
  - flake8

after_success:
  - coveralls
